SVRERP02 Implementation Plan Codebase Refactoring &amp; Hardening Revised by Claude Team Analysis Version 2.0 | 5 Phases | 15 Changes | 4 Critical Fixes Important Notices ⚠ RLS Policy Changes — User Review Required Transitioning all RLS policies from USING(true) to role-based policies requires proper JWT role mappings in Supabase. Users must have app_metadata containing {&quot;role&quot;: &quot;ADMIN&quot; | &quot;MANAGER&quot; | &quot;QC&quot; | &quot;OPERATOR&quot; | &quot;EDITOR&quot;}. A helper function auth.user_role() will be created first (Phase 0) to ensure safe migration. 📋 Open Question — Notification System Backend The NotificationPanel (Phase 3) requires a &quot;notifications&quot; table in the database. This table schema needs confirmation before implementation. A suggested schema is provided in Section 5. What Changed from Original Plan The original plan covered 5 important steps but missed several critical issues. Below is a comparison of what was added and why. ✓ Original Plan (Kept) + Added in Revised Plan ✓ AppShell universal sidebar component + tokens.css — design foundation (Phase 0) ✓ batch_components table migration + auth.user_role() SQL helper (Phase 0) ✓ RLS hardening (60+ policies) + Forgot Password flow in LoginPage ✓ Supabase Realtime for CCP logs + BatchComponent type safety in bosApi.ts ✓ signature_pad stable package + NotificationPanel — click-to-open + Generic Excel/CSV export utility + RLS role-based automated test script + Performance: lazy split + pagination Proposed Changes The plan is structured into 5 phases . Phases must be completed in order — each phase is a dependency for the next. Phase 0 — Foundation (Must complete first) These two items are prerequisites for everything else. Without tokens.css, AppShell will repeat the same hardcoding problem. Without the SQL helper, RLS policies will break for some user types. Type File / Target What Changes Priority NEW src/app/styles/tokens.css Single source of truth for all colors: --bg-main, --bg-sidebar, --color-accent, --color-status-*, --color-border. Eliminates #071526 hardcoded in 11+ files. Critical NEW supabase/migrations/00_user_role_helper.sql CREATE OR REPLACE FUNCTION auth.user_role() — reads from app_metadata.role with safe OPERATOR fallback. Used by all 60+ hardened RLS policies. Critical Phase 1 — Structure &amp; UI 1.1 Universal AppShell Component Replaces 11 duplicate sidebar implementations. Estimated reduction: ~8,000 lines of duplicated code. Type File / Target What Changes Priority NEW src/app/components/shell/AppShell.tsx Flexible layout shell. Accepts nav[], module config, optional badge count. Mobile overlay, smooth transition. Uses CSS variables from tokens.css. High MODIFY src/app/layouts/ (all 11 files) AccountsLayout, AdminLayout, CmsLayout, CompliancesLayout, DmsLayout, FsmsLayout, InventoryLayout, LogisticsLayout, ProductionLayout, QcLayout, RndLayout — each refactored to wrap &lt;AppShell&gt;. High 1.2 SVG Icons — Remove All Emojis from Nav InventoryLayout and several other modules use emoji icons ( 📦 📥 🔍 ). These are inconsistent on screen readers, blur on Windows ClearType, and cannot be styled via CSS. Type File / Target What Changes Priority MODIFY src/app/layouts/InventoryLayout.tsx Replace emoji icon strings with inline SVG icons in INV_NAV config. Pattern: &lt;svg width={16} height={16} fill=&quot;none&quot; stroke=&quot;currentColor&quot;&gt;. Medium MODIFY src/app/layouts/ProductionLayout.tsx Same SVG icon replacement for all nav items. Medium MODIFY src/app/layouts/LogisticsLayout.tsx Same SVG icon replacement for all nav items. Medium 1.3 Forgot Password — LoginPage.tsx Missing entirely from original plan. Production ERP without password reset is a blocker. Type File / Target What Changes Priority MODIFY src/app/modules/auth/LoginPage.tsx Add stage: &quot;login&quot; | &quot;forgot&quot; | &quot;reset&quot;. Forgot form calls supabase.auth.resetPasswordForEmail(). Success screen with instructions. Add dashboard skeleton to prevent blank flash on redirect. High 1.4 Central Route Guards Type File / Target What Changes Priority NEW src/app/components/RequireAuth.tsx Single component: checks isAuthed, loading state, and canAccess(minRole). Returns &lt;Navigate to=&quot;/login&quot;&gt; or &lt;Navigate to=&quot;/unauthorized&quot;&gt; or &lt;Outlet&gt;. Replaces scattered per-module auth checks. Medium MODIFY src/app/App.tsx Wrap all ERP routes inside &lt;RequireAuth minRole=&quot;...&quot;&gt; groups. Clean up redundant per-page auth checks. Medium Phase 2 — Database &amp; Security 2.1 Missing batch_components Table Data loss bug. bosApi.ts queries this table at lines 75-77 but it does not exist in the database. Batch ingredient data is silently not being saved. Type File / Target What Changes Priority NEW supabase/migrations/01_create_batch_components.sql CREATE TABLE batch_components: id, batch_id (FK→batches), lot_id (FK→lots), ingredient TEXT, planned_kg NUMERIC, actual_kg NUMERIC, variance_pct GENERATED ALWAYS AS computed column, added_by, added_at. Indices on batch_id and lot_id. RLS enabled. Critical MODIFY src/app/lib/bosApi.ts Add BatchComponent TypeScript interface. Replace &quot;as any&quot; type casts at lines 75-77 with typed BatchComponent queries. High 2.2 RLS Security Hardening — 60+ Policies All current policies use USING(true) which means any authenticated user can read/write any data. Role-based access must be enforced at the database level. Type File / Target What Changes Priority NEW supabase/migrations/02_harden_rls_policies.sql Rewrite policies for: lots, batches, grns, fg_lots, stock_ledger, ccp_logs, prp_logs, qc_checks, capas, training_records, sop_documents. SELECT for authenticated users, INSERT/UPDATE for OPERATOR+, DELETE for MANAGER/ADMIN only. Critical NEW scripts/test-rls-roles.ts Automated test: creates test sessions for QC, OPERATOR, MANAGER, ADMIN roles. Attempts restricted operations for each. Asserts correct allow/deny behavior. Run after migration. High Phase 3 — Missing Features 3.1 Notification Panel Missing from original plan. Unread count badge exists but clicking it does nothing. Dead UI element. Type File / Target What Changes Priority NEW src/app/components/NotificationPanel.tsx Popover component triggered by bell icon. Lists notifications with unread state, timestamp, action_url. markRead(id) and markAllRead() actions. Requires &quot;notifications&quot; table (see schema note in Section 5). Medium MODIFY src/app/components/AdminLayout.tsx + AppShell.tsx Wire NotificationPanel into header area. Replace static badge with interactive bell. Medium 3.2 Data Export Utility Missing from original plan. Accounts, Inventory, Logistics modules show data but have no export. Common need for Finance and Operations teams. Type File / Target What Changes Priority NEW src/app/lib/export.ts Generic exportToExcel(data, columns, filename) utility. Uses xlsx library (already in dependencies). Adds today&apos;s date to filename. Column config: { header: string, getValue: (row) =&gt; string }. Medium MODIFY src/app/modules/accounts/AccountsDashboard.tsx Add Export Excel button using exportToExcel(). Column config for invoices/expenses. Low MODIFY src/app/modules/inventory/Store.tsx Add Export button for stock data. Low MODIFY src/app/modules/logistics/FgStore.tsx Add Export button for FG lot data. Low 3.3 Performance — Bundle Split &amp; Pagination Missing from original plan. Low severity but will hurt at scale. Type File / Target What Changes Priority MODIFY src/app/App.tsx Make all public pages lazy: const HomePage = lazy(() =&gt; import(...)). Currently 44KB+ of public page code loads for every ERP-only user session. Low MODIFY vite.config.ts Add manualChunks: { public: [...], production: [...], rnd: [...], fsms: [...] } to Rollup options for proper code splitting. Low MODIFY src/app/modules/logistics/FgStore.tsx + GeneralStore.tsx + RmStore.tsx Replace .select(&quot;*&quot;) without limit with paginated queries: .limit(50).range(page*50, (page+1)*50-1). Add &quot;Load More&quot; button. Medium Phase 4 — Realtime &amp; DMS These are from the original plan, now placed in Phase 4 after structural and security work is complete. 4.1 Supabase Realtime for CCP Logs LiveCcpMonitor.tsx currently polls every 30,000ms . Food safety critical control points require actual real-time data, not 30-second stale values. Type File / Target What Changes Priority MODIFY src/app/modules/fsms/HaccpLog.tsx Replace setInterval(30_000) polling with supabase.channel(&quot;ccp-realtime&quot;).on(&quot;postgres_changes&quot;, { event: &quot;INSERT&quot;, table: &quot;ccp_logs&quot; }, handler).subscribe(). New records appear instantly. Cleanup on unmount. High MODIFY src/app/modules/fsms/LiveCcpMonitor.tsx Same Realtime subscription pattern. Add visual &quot;Live&quot; indicator (green dot) that pulses when connected. Medium 4.2 Stable Signature Pad in DMS Current package react-signature-canvas@1.1.0-alpha.2 is an alpha release. Breaking changes can arrive at any update. Type File / Target What Changes Priority MODIFY package.json Remove react-signature-canvas@^1.1.0-alpha.2 and @types/react-signature-canvas. Add signature_pad@^5.0.4 (stable, 3M+ weekly downloads). Medium MODIFY src/app/modules/dms/DmsCreate.tsx Refactor signature canvas to use signature_pad directly via useRef&lt;SignaturePad&gt;. Add post-signature verification trail: signer name, timestamp, hash prefix, IP address. Add &quot;Verify this signature&quot; link. Medium Verification Plan Every phase must pass its verification step before the next phase begins. Phase Test Type What to Verify Command / Action Phase 0 Manual CSS variables resolve in browser DevTools. auth.user_role() returns correct role for test users. DevTools → Computed → check --color-accent Phase 1 Build npm run build — zero TypeScript errors. All 11 layouts render with AppShell. npm run build Phase 1 Manual Login as each role. Verify sidebar shows only permitted nav items. Forgot Password flow completes. Test with QC, OPERATOR, ADMIN users Phase 2 Migration batch_components table exists. Saving a batch with ingredients stores rows correctly. psql: SELECT * FROM batch_components LIMIT 5 Phase 2 Automated RLS role test script passes for all role/operation combinations. npx ts-node scripts/test-rls-roles.ts Phase 3 Manual Notification bell opens panel. Export button downloads .xlsx file with correct data. Click bell → verify panel. Click Export → open file. Phase 3 Performance Bundle size reduced. FG Store loads 50 records, not all. npm run build — check dist/ chunk sizes Phase 4 Manual Log a new CCP reading — it appears in monitor immediately without reload. Open LiveCcpMonitor, submit new CCP log Phase 4 Manual Sign a DMS document. Verification trail shows name, timestamp, hash. DMS → Create → Sign → verify trail All Regression npm test passes. No console errors on any module page. npm test Summary: All 15 Changes # Category Problem Claude Fix Phase 1 Architecture 11 duplicate sidebar implementations (~8,000 lines) Single AppShell component 1 — Structure 2 Architecture Hex values hardcoded in 11+ files (#071526, #EAB308) tokens.css design file 0 — Foundation 3 Security RLS USING(true) on 60+ policies — no data isolation JWT role-based RLS policies 2 — Database 4 Bug batch_components table missing — ingredient data lost Migration + typed API 2 — Database 5 UX No Forgot Password flow — admin must manually reset Forgot/reset in LoginPage 1 — Structure 6 UX CCP &quot;Live&quot; monitor polls every 30s — stale food safety data Supabase Realtime channel 4 — Realtime 7 UX No real-time % feedback in Formula Builder Live percentage meter 3 — Features 8 UX Notification badge exists — panel does nothing on click NotificationPanel popover 3 — Features 9 UX No data export anywhere in Accounts/Inventory/Logistics Generic Excel export utility 3 — Features 10 Architecture Emoji icons in nav — accessibility + rendering issues SVG icon system 1 — Structure 11 Architecture Direct Supabase calls in components — untestable Repository pattern + types 2 — Database 12 Architecture Route guards scattered per-module Central RequireAuth component 1 — Structure 13 Performance Public pages (~44KB) load for every ERP user Lazy split + manualChunks 3 — Features 14 Performance .select(&quot;*&quot;) without limit — memory risk at scale Cursor pagination (limit 50) 3 — Features 15 DMS Alpha package react-signature-canvas@1.1.0-alpha.2 Stable signature_pad@5.0.4 4 — Realtime Schema Note — Notifications Table 📌 Required before Phase 3, Step 3.1 The NotificationPanel requires a notifications table. Suggested schema below. Confirm this matches your data model before implementing. Suggested SQL: CREATE TABLE notifications ( id UUID PRIMARY KEY DEFAULT gen_random_uuid(), user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE, message TEXT NOT NULL, action_url TEXT, read BOOLEAN DEFAULT false, created_at TIMESTAMPTZ DEFAULT NOW() ); CREATE INDEX idx_notifications_user ON notifications(user_id); ALTER TABLE notifications ENABLE ROW LEVEL SECURITY; -- Users can only see their own notifications CREATE POLICY notif_select ON notifications FOR SELECT USING (user_id = auth.uid());
